apiVersion: scaffolder.backstage.io/v1beta3
kind: Template
metadata:
  name: dropdown-yaml-generator
  title: Dropdown YAML Generator
  description: Generates a YAML file based on dropdown selection
spec:
  owner: user:guest
  type: service
  parameters:
    - title: Configuration
      required:
        - resourceType
        - name
      properties:
        name:
          title: Resource Name
          type: string
          description: Name of the resource
        resourceType:
          title: Resource Type
          type: string
          description: Select the type of resource
          enum: ["server", "database", "queue"]  # Dropdown options
          default: "server"  # Default selection
          ui:widget: select  # Ensures it renders as a dropdown
  steps:
    - id: generate-yaml
      name: Generate YAML with Custom Action
      action: custom:generate-yaml  # Custom action we'll define
      input:
        name: ${{ parameters.name }}
        resourceType: ${{ parameters.resourceType }}
    - id: publish
      name: Publish to GitHub (Optional)
      action: publish:github
      input:
        allowedHosts: ["github.com"]
        repoUrl: ${{ parameters.repoUrl }}  # Optional: add repoUrl to parameters if publishing
  output:
    links:
      - title: Generated YAML
        url: ${{ steps['publish'].output.remoteUrl || 'local-output' }}
